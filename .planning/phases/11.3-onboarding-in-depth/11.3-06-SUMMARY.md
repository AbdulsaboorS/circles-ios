---
plan: 11.3-06
status: complete
completed: 2026-04-03
commit: 89e0895
---

## Summary

Rewrote ContentView to implement auth-last routing and wired both onboarding coordinators end-to-end. Added the Soul Gate replacement nudge banner in HomeView.

## What Was Built

**ContentView (auth-last routing):**
- Unauthenticated users now enter `AmiirOnboardingFlowView` directly — `AuthView` is gone from the default path
- Invite code present → `MemberOnboardingFlowView` with pre-filled `inviteCodeInput`
- `memberCoordinator` is always-initialized `@State` (no Optional), matching `MemberOnboardingCoordinator.init()`
- `isJoinerFlow` computed var: `shouldSwitchToJoinerFlow || pendingInviteCode != nil`
- `handlePostAuth` triggers `flushToSupabase` on the correct coordinator when `auth.isAuthenticated` fires
- `isFlushingToSupabase` shows loading screen during Supabase writes post-auth
- Returning users (hasCompletedOnboarding) route directly to `MainTabView`

**HomeView (invite nudge banner):**
- Gold-bordered banner below heartSection, above habitsSection
- Shown when `should_show_invite_nudge_{userId}` = true AND `invite_nudge_dismissed_{userId}` = false
- Share button opens `ShareSheet` with circle invite URL (fetched from `CircleService.fetchMyCircles`)
- X dismiss sets `invite_nudge_dismissed_{userId}` in UserDefaults with animation

**AmiirOnboardingCoordinator:**
- `completeOnboarding(userId:)` now also sets `should_show_invite_nudge_{userId}` = true

## Key Files

- `Circles/ContentView.swift` — full rewrite, auth-last routing
- `Circles/Home/HomeView.swift` — nudge banner + loadNudgeState
- `Circles/Onboarding/AmiirOnboardingCoordinator.swift` — nudge flag in completeOnboarding

## Self-Check: PASSED

- BUILD SUCCEEDED (zero errors)
- AuthView() — 0 occurrences in ContentView body
- memberCoordinator non-Optional ✓
- flushToSupabase in handlePostAuth ✓
- MemberOnboardingFlowView + AmiirOnboardingFlowView both present ✓
- showInviteNudge + UserDefaults keys in HomeView ✓
- should_show_invite_nudge_ in AmiirOnboardingCoordinator ✓
