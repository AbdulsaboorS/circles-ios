---
plan: 11.3-03
status: complete
completed: "2026-04-02"
---

# 11.3-03 Summary — Amir Coordinator Rewrite (Auth-Last)

## What was done

**AmiirOnboardingCoordinator.swift** — complete rewrite:
- Step enum: 8 cases (coreHabits, circleIdentity, transitionToPersonal, personalIntentions, transitionToAI, aiGeneration, foundation, activation); removed `landing` and `soulGate`
- Added `shouldSwitchToJoinerFlow: Bool` property
- Added navigation helpers: `proceedToIdentity()`, `proceedToTransitionToPersonal()`, `proceedToTransitionToAI()`, `proceedToAIGeneration()`, `proceedToFoundation()`
- `proceedToActivation()` now calls `savePendingState()` before appending
- Added `flushToSupabase(userId:)` — post-auth Supabase writer (circle + habits + AI plans)
- Added `savePendingState()` private helper using `OnboardingPendingState.save()`
- `completeOnboarding(userId:)` simplified to UserDefaults write + `isComplete = true` (AI plans fire in flush)
- `fireBackgroundPlans()` is a documented no-op pre-auth
- Removed `createCircleAndProceed(userId:)`

**AmiirOnboardingFlowView.swift** — complete rewrite:
- Root of NavigationStack is now `AmiirLandingSanctuaryView()` (was `AmiirStep1IdentityView`)
- Routes all 8 Step cases including `OnboardingTransitionView` for both transition cases
- Uses `OnboardingTransitionQuote` constants

**AmiirStep2HabitsView.swift**:
- `StepIndicator(current: 1, total: 5)` → `StepIndicator(current: 1, total: 7)`
- CTA: `proceedToPersonalIntentions()` → `proceedToIdentity()`

**AmiirStep1IdentityView.swift**:
- `StepIndicator(current: 0, total: 5)` → `StepIndicator(current: 2, total: 7)`
- CTA: `proceedToHabits()` → `proceedToTransitionToPersonal()`

**AmiirStep3PersonalView.swift**:
- Personal habit cap: 3 → 2 (`count < 2`, `count == 2`, "Maximum 2 intentions")
- `StepIndicator(current: 2, total: 5)` → `StepIndicator(current: 3, total: 7)`
- CTA + Skip: `proceedToLocation()` → `proceedToTransitionToAI()`

**AmiirStep3LocationView.swift** (Foundation step):
- Removed `@Environment(AuthManager.self)` and `import Supabase`
- City tap: replaced `createCircleAndProceed(userId:)` Task → `coordinator.proceedToActivation()`
- Added push notification soft ask: bell icon, "Enable the Adhan" label, Enable button (calls `NotificationService.shared.requestPermission()`), denied fallback with Settings deep-link
- `StepIndicator(current: 3, total: 5)` → `StepIndicator(current: 5, total: 7)`

## Build result
`BUILD SUCCEEDED` — zero errors
