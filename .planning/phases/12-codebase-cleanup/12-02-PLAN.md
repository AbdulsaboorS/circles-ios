---
phase: 12-codebase-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - Circles/DesignSystem/Components.swift
  - Circles/DesignSystem/AppBackground.swift
  - Circles/Circles/CircleDetailView.swift
  - Circles/Services/RoadmapGenerationFlag.swift
  - Circles/Services/HabitPlanService.swift
  - Circles/Home/HomeView.swift
  - Circles/Onboarding/AmiirOnboardingCoordinator.swift
  - Circles/Onboarding/MemberOnboardingCoordinator.swift
autonomous: true
requirements:
  - CLEANUP-02

must_haves:
  truths:
    - "Build compiles with zero errors after all changes"
    - "SectionHeader still renders correctly in CircleDetailView Activity section"
    - "RoadmapGenerationFlag logic is inlined into HabitPlanService — the separate file is gone"
    - "HomeView still shows/hides the roadmap banner correctly"
    - "AppCard, PrimaryButton, ChipButton, AppBackground are deleted from Components.swift and AppBackground.swift — only SectionHeader remains in Components.swift"
  artifacts:
    - path: "Circles/DesignSystem/Components.swift"
      provides: "Only SectionHeader component"
      contains: "struct SectionHeader"
    - path: "Circles/Services/HabitPlanService.swift"
      provides: "Inlined RoadmapGenerationFlag logic as private static methods"
      contains: "roadmap_generating_"
  key_links:
    - from: "Circles/Home/HomeView.swift"
      to: "HabitPlanService.isRoadmapGenerating(userId:)"
      via: "static method call replacing RoadmapGenerationFlag.isActive"
      pattern: "HabitPlanService.isRoadmapGenerating"
    - from: "Circles/Onboarding/AmiirOnboardingCoordinator.swift"
      to: "HabitPlanService.setRoadmapGenerating / clearRoadmapGenerating"
      via: "static method calls replacing RoadmapGenerationFlag.set/clear"
      pattern: "HabitPlanService.setRoadmapGenerating|HabitPlanService.clearRoadmapGenerating"
---

<objective>
Audit and prune Components.swift and AppBackground.swift (remove unused wrappers, keep only SectionHeader), then fold RoadmapGenerationFlag into HabitPlanService as private static helpers.

Purpose: Remove the last design-system dead weight and eliminate the 25-line one-trick enum file by inlining its logic where it belongs.

Output: Components.swift contains only SectionHeader; AppBackground.swift is deleted; RoadmapGenerationFlag.swift is deleted; HabitPlanService has inlined generation-flag methods; build is clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-codebase-cleanup/12-01-SUMMARY.md

<interfaces>
<!-- Key interfaces the executor needs. No exploration required. -->

From Circles/Services/RoadmapGenerationFlag.swift (to be inlined):
```swift
enum RoadmapGenerationFlag {
    static func set(userId: UUID)          // stores Date timestamp in UserDefaults
    static func clear(userId: UUID)        // removes key from UserDefaults
    static func isActive(userId: UUID) -> Bool  // true if flag set AND < 5 minutes old
    // key format: "roadmap_generating_{userId.uuidString}"
}
```

Call sites:
- HomeView.swift:252,260: RoadmapGenerationFlag.isActive(userId: uid)
- AmiirOnboardingCoordinator.swift:161: RoadmapGenerationFlag.set(userId: userId)
- AmiirOnboardingCoordinator.swift:166: RoadmapGenerationFlag.clear(userId: userId)
- MemberOnboardingCoordinator.swift:145: RoadmapGenerationFlag.set(userId: userId)
- MemberOnboardingCoordinator.swift:150: RoadmapGenerationFlag.clear(userId: userId)

From Circles/DesignSystem/Components.swift (audit result):
- SectionHeader — LIVE: used at CircleDetailView.swift:77 as `SectionHeader(title: "Activity")`
- AppCard — DEAD: no usage outside Components.swift
- PrimaryButton — DEAD: no usage outside Components.swift
- ChipButton — DEAD: no usage outside Components.swift
- AppBackground — DEAD: only used inside Components.swift itself (in previews) and AppBackground.swift

From Circles/DesignSystem/AppBackground.swift:
- AppBackground struct — DEAD: used only in its own PreviewProvider and inside Components.swift
- No live view references AppBackground() outside these two files
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline RoadmapGenerationFlag into HabitPlanService, delete the enum file</name>
  <read_first>
    - Circles/Services/RoadmapGenerationFlag.swift
    - Circles/Services/HabitPlanService.swift
    - Circles/Home/HomeView.swift
    - Circles/Onboarding/AmiirOnboardingCoordinator.swift
    - Circles/Onboarding/MemberOnboardingCoordinator.swift
  </read_first>
  <files>
    Circles/Services/HabitPlanService.swift,
    Circles/Home/HomeView.swift,
    Circles/Onboarding/AmiirOnboardingCoordinator.swift,
    Circles/Onboarding/MemberOnboardingCoordinator.swift,
    Circles/Services/RoadmapGenerationFlag.swift,
    Circles/Circles.xcodeproj/project.pbxproj
  </files>
  <action>
    Step 1 — Add 3 static methods to HabitPlanService (at the bottom of the class, before the closing brace),
    preserving identical logic from RoadmapGenerationFlag:

    ```swift
    // MARK: - Roadmap generation flag (inlined from RoadmapGenerationFlag)

    private static func roadmapFlagKey(userId: UUID) -> String {
        "roadmap_generating_\(userId.uuidString)"
    }

    static func setRoadmapGenerating(userId: UUID) {
        UserDefaults.standard.set(Date().timeIntervalSince1970, forKey: roadmapFlagKey(userId: userId))
    }

    static func clearRoadmapGenerating(userId: UUID) {
        UserDefaults.standard.removeObject(forKey: roadmapFlagKey(userId: userId))
    }

    /// Returns true while the flag is set AND is less than 5 minutes old.
    static func isRoadmapGenerating(userId: UUID) -> Bool {
        guard let ts = UserDefaults.standard.object(forKey: roadmapFlagKey(userId: userId)) as? TimeInterval else {
            return false
        }
        return Date().timeIntervalSince1970 - ts < 300
    }
    ```

    Step 2 — Update HomeView.swift:
      Replace: RoadmapGenerationFlag.isActive(userId: uid)
      With:    HabitPlanService.isRoadmapGenerating(userId: uid)
      (Two occurrences at lines ~252 and ~260)

    Step 3 — Update AmiirOnboardingCoordinator.swift:
      Replace: RoadmapGenerationFlag.set(userId: userId)
      With:    HabitPlanService.setRoadmapGenerating(userId: userId)
      Replace: RoadmapGenerationFlag.clear(userId: userId)
      With:    HabitPlanService.clearRoadmapGenerating(userId: userId)

    Step 4 — Update MemberOnboardingCoordinator.swift:
      Replace: RoadmapGenerationFlag.set(userId: userId)
      With:    HabitPlanService.setRoadmapGenerating(userId: userId)
      Replace: RoadmapGenerationFlag.clear(userId: userId)
      With:    HabitPlanService.clearRoadmapGenerating(userId: userId)

    Step 5 — Delete RoadmapGenerationFlag.swift from disk:
      rm Circles/Services/RoadmapGenerationFlag.swift

    Step 6 — Remove RoadmapGenerationFlag.swift from project.pbxproj:
      grep -n "RoadmapGenerationFlag" Circles/Circles.xcodeproj/project.pbxproj
      Remove all matching lines (PBXFileReference, PBXBuildFile, group children UUID, sources UUID).

    Step 7 — Build check:
      xcodebuild -project Circles/Circles.xcodeproj -scheme Circles -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20
  </action>
  <verify>
    <automated>grep -rn "RoadmapGenerationFlag" Circles/ --include="*.swift" --include="*.pbxproj" | grep -v "^Binary"</automated>
  </verify>
  <acceptance_criteria>
    - grep returns no results for RoadmapGenerationFlag
    - grep -n "isRoadmapGenerating\|setRoadmapGenerating\|clearRoadmapGenerating" Circles/Services/HabitPlanService.swift returns 3 method definitions
    - grep -n "HabitPlanService.isRoadmapGenerating" Circles/Home/HomeView.swift returns 2 lines
    - xcodebuild BUILD SUCCEEDED
  </acceptance_criteria>
  <done>RoadmapGenerationFlag inlined into HabitPlanService, enum file deleted, all call sites updated, build clean</done>
</task>

<task type="auto">
  <name>Task 2: Prune Components.swift (delete dead wrappers), delete AppBackground.swift</name>
  <read_first>
    - Circles/DesignSystem/Components.swift
    - Circles/DesignSystem/AppBackground.swift
    - Circles/Circles/CircleDetailView.swift
  </read_first>
  <files>
    Circles/DesignSystem/Components.swift,
    Circles/DesignSystem/AppBackground.swift,
    Circles/Circles.xcodeproj/project.pbxproj
  </files>
  <action>
    Step 1 — Confirm no live files reference AppCard, PrimaryButton, ChipButton outside Components.swift:
      grep -rn "AppCard\|PrimaryButton\b\|ChipButton" Circles/ --include="*.swift" | grep -v "Components.swift"
    If any results appear, STOP and report before proceeding.

    Step 2 — Confirm AppBackground is not referenced outside Components.swift and AppBackground.swift:
      grep -rn "AppBackground" Circles/ --include="*.swift" | grep -v "Components.swift" | grep -v "AppBackground.swift"
    If any results appear, STOP and report before proceeding.

    Step 3 — Rewrite Components.swift to contain ONLY SectionHeader (the one live component).
    Preserve the exact SectionHeader struct definition and its MARK comment. Remove AppCard,
    PrimaryButton, ChipButton, and their preview/preview providers. The new file should be:

    ```swift
    import SwiftUI

    // MARK: - SectionHeader

    /// Simple section title label used within card content areas.
    struct SectionHeader: View {
        let title: String

        var body: some View {
            Text(title)
                .font(.appSubheadline)
                .foregroundStyle(Color.msTextPrimary)
                .frame(maxWidth: .infinity, alignment: .leading)
        }
    }
    ```

    IMPORTANT: Read the actual SectionHeader definition from the current Components.swift before writing.
    Copy it verbatim — do not alter behavior. SectionHeader uses Color.msTextPrimary which is a private
    extension in the calling file (CircleDetailView). Since the token will be consolidated in Plan 03,
    and for now the token is still private-per-file, use the exact current SectionHeader body.
    If SectionHeader currently uses a different color token, preserve it as-is.

    Step 4 — Delete AppBackground.swift from disk:
      rm Circles/DesignSystem/AppBackground.swift

    Step 5 — Remove AppBackground.swift from project.pbxproj:
      grep -n "AppBackground" Circles/Circles.xcodeproj/project.pbxproj
      Remove all matching lines (PBXFileReference, PBXBuildFile, group children UUID, sources UUID).

    Step 6 — Build check:
      xcodebuild -project Circles/Circles.xcodeproj -scheme Circles -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20
  </action>
  <verify>
    <automated>grep -rn "AppCard\|PrimaryButton\b\|AppBackground" Circles/ --include="*.swift" | grep -v "^Binary"</automated>
  </verify>
  <acceptance_criteria>
    - grep returns no results for AppCard, PrimaryButton, AppBackground
    - Components.swift exists and contains only SectionHeader (wc -l returns < 25 lines)
    - AppBackground.swift does not exist on disk
    - CircleDetailView still compiles — SectionHeader is still defined in Components.swift
    - xcodebuild BUILD SUCCEEDED
  </acceptance_criteria>
  <done>Dead design system components removed, AppBackground.swift deleted, SectionHeader preserved, build clean</done>
</task>

<task type="human">
  <name>Task 3: Simulator smoke test — confirm no runtime regressions</name>
  <read_first>
    - (none — manual verification step)
  </read_first>
  <files></files>
  <action>
    This plan touched Components.swift (SectionHeader must still render), deleted
    AppBackground.swift, and inlined RoadmapGenerationFlag into HabitPlanService.
    A clean build doesn't catch silent UI breaks or missing components at runtime.

    Step 1 — Launch on simulator:
      xcrun simctl launch booted app.joinlegacy
      (If not already booted from Plan 01 smoke test, boot first:
       xcrun simctl boot "iPhone 16" && xcrun simctl launch booted app.joinlegacy)

    Step 2 — Check the screens most likely to regress from this plan's changes:

      [ ] Home tab — loads without blank screen
          Specifically: tap a habit to open HabitDetailView. The AI roadmap
          banner/loading state should still appear if applicable — this is driven
          by HabitPlanService.isRoadmapGenerating (the inlined flag).

      [ ] CircleDetailView — navigate into a circle. The "Activity" section header
          should render (SectionHeader is used here). If it shows blank where
          "Activity" used to be, SectionHeader broke.

      [ ] Amir onboarding path (if testable) — the roadmap flag set/clear calls
          in AmiirOnboardingCoordinator must still work. If you can reach the AI
          Generation step in onboarding, confirm no crash.

    Step 3 — Confirm no white/blank sections:
      [ ] "Activity" label visible in CircleDetailView
      [ ] Home AI roadmap banner logic not broken (app doesn't crash on HomeView load)
      [ ] No tab shows a completely blank white view

    If anything is broken: STOP, do not commit, report the issue.
  </action>
  <verify>
    <automated>xcrun simctl launch booted app.joinlegacy 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - App launches without crashing
    - CircleDetailView "Activity" section header is visible (SectionHeader renders)
    - HomeView loads without crash (RoadmapGenerationFlag inline works)
    - No blank/white screens on any of the 5 tabs
  </acceptance_criteria>
  <done>SectionHeader renders in CircleDetailView, HomeView loads correctly, no crashes or blank screens</done>
</task>

</tasks>

<verification>
Final check after all tasks:
  grep -rn "RoadmapGenerationFlag\|AppCard\|PrimaryButton\b\|AppBackground\b" Circles/ --include="*.swift" --include="*.pbxproj" | grep -v "^Binary"

Must return no output. Then verify build is clean.
</verification>

<success_criteria>
- RoadmapGenerationFlag.swift deleted; its 3 methods live in HabitPlanService
- Components.swift contains only SectionHeader (< 25 lines)
- AppBackground.swift deleted
- All call sites updated to use HabitPlanService static methods
- xcodebuild BUILD SUCCEEDED with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-codebase-cleanup/12-02-SUMMARY.md`
</output>
