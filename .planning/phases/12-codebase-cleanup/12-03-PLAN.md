---
phase: 12-codebase-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
files_modified:
  - Circles/DesignSystem/DesignTokens.swift
  - Circles/DesignSystem/ThemeManager.swift
  - Circles/CirclesApp.swift
  - Circles/Home/HomeView.swift
  - Circles/Home/HabitDetailView.swift
  - Circles/Home/AddPrivateIntentionSheet.swift
  - Circles/Auth/AuthView.swift
  - Circles/Community/CommunityView.swift
  - Circles/Circles/CircleDetailView.swift
  - Circles/Circles/CreateCircleView.swift
  - Circles/Circles/JoinCircleView.swift
  - Circles/Moment/MomentCameraView.swift
  - Circles/Moment/MomentPreviewView.swift
  - Circles/Profile/ProfileView.swift
  - Circles/Onboarding/AmiirLandingSanctuaryView.swift
  - Circles/Onboarding/AmiirStep3PersonalView.swift
  - Circles/Onboarding/AmiirStep1IdentityView.swift
  - Circles/Onboarding/AmiirStep2HabitsView.swift
  - Circles/Onboarding/AmiirStep3LocationView.swift
  - Circles/Onboarding/AmiirAIGenerationView.swift
  - Circles/Onboarding/AmiirActivationView.swift
  - Circles/Onboarding/OnboardingTransitionView.swift
  - Circles/Onboarding/CirclePreviewView.swift
  - Circles/Onboarding/JoinerLandingView.swift
  - Circles/Onboarding/JoinerCircleAlignmentView.swift
  - Circles/Onboarding/JoinerPersonalHabitsView.swift
  - Circles/Onboarding/JoinerAIGenerationView.swift
  - Circles/Onboarding/JoinerIdentityView.swift
  - Circles/Onboarding/JoinerAuthGateView.swift
autonomous: true
requirements:
  - CLEANUP-03
  - CLEANUP-04

must_haves:
  truths:
    - "Build compiles with zero errors after all changes"
    - "App always launches in dark mode — no light mode, no NOAA solar switching"
    - "Every file that previously had a private extension Color with MS tokens now uses Color.ms* from the shared DesignTokens extension instead"
    - "DesignTokens.swift defines all MS tokens as internal (module-level) Color extensions"
    - "ThemeManager is a trivial dark-mode-only class — no timers, no solar math, no ThemeMode enum"
  artifacts:
    - path: "Circles/DesignSystem/DesignTokens.swift"
      provides: "Shared MS token definitions for entire module"
      contains: "static let msBackground"
    - path: "Circles/DesignSystem/ThemeManager.swift"
      provides: "Dark-mode-only theme manager"
      contains: "colorScheme = .dark"
  key_links:
    - from: "Circles/DesignSystem/DesignTokens.swift"
      to: "all Swift files in module"
      via: "internal Color extension — no import needed, same module"
      pattern: "extension Color.*msBackground"
    - from: "Circles/CirclesApp.swift"
      to: "ThemeManager.shared"
      via: ".preferredColorScheme(themeManager.colorScheme)"
      pattern: "preferredColorScheme"
---

<objective>
Consolidate the 29 per-file `private extension Color` MS token blocks into a single shared internal extension in DesignTokens.swift, then simplify ThemeManager to a trivial dark-mode enforcer.

Purpose: One source of truth for all MS color tokens. Eliminates ~200 lines of duplicated definitions spread across 29 files. Removes the unused NOAA solar switching infrastructure.

Output: DesignTokens.swift gains a comprehensive MS token extension; all 29 files drop their private blocks; ThemeManager becomes a 15-line singleton that always returns .dark; build is clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-codebase-cleanup/12-01-SUMMARY.md
@.planning/phases/12-codebase-cleanup/12-02-SUMMARY.md

<interfaces>
<!-- Complete MS token inventory across all live files. Executor needs this to write the shared extension. -->

Core 6 tokens (all 29 files define these):
  msBackground     = Color(hex: "1A2E1E")
  msCardShared     = Color(hex: "243828")
  msGold           = Color(hex: "D4A240")
  msTextPrimary    = Color(hex: "F0EAD6")
  msTextMuted      = Color(hex: "8FAF94")
  msBorder         = Color(hex: "D4A240").opacity(0.18)   // some files use 0.28 — use 0.18 as canonical

Additional tokens (HomeView only — must also be shared):
  msBackgroundDeep = Color(hex: "131C14")
  msCardPersonal   = Color(hex: "1E3122")
  msCardDone       = Color(hex: "2A4A30")
  msCardWarm       = Color(hex: "201C14")
  msCardWarmDone   = Color(hex: "2E2410")

Additional tokens (HabitDetailView + AddPrivateIntentionSheet):
  msCardDeep       = Color(hex: "1E3122")   // same hex as msCardPersonal — use one name

IMPORTANT: msCardPersonal and msCardDeep have the same hex value (#1E3122).
Define ONE token: `msCardDeep = Color(hex: "1E3122")`. Update HomeView's msCardPersonal usages
to use Color.msCardDeep after adding it to DesignTokens.

Swift 6 / Xcode 26 note from STATE.md:
  "Xcode 26 / Swift 6 requires explicit `Color.msToken` prefix — shorthand dot syntax (`.msGold`)
  fails to infer Color when the expected type is ShapeStyle."
  All usages already use `Color.msBackground` etc. — the shared extension does NOT change this pattern.

ThemeManager.swift current state:
  - @Observable @MainActor final class ThemeManager
  - var colorScheme: ColorScheme = .dark
  - var mode: ThemeMode = .alwaysLight (note: default is light, but app hardcodes dark visually)
  - scheduleAutoSwitch() — called in CirclesApp.swift on appear
  - SolarCalculator private enum — 100+ lines of NOAA math
  - ThemeMode enum: auto, alwaysLight, alwaysDark

ThemeManager call sites:
  - CirclesApp.swift:45: @State private var themeManager = ThemeManager.shared
  - CirclesApp.swift:59: themeManager.scheduleAutoSwitch()
  - ContentView.swift:6: @Environment(ThemeManager.self) private var themeManager
  - ContentView.swift:28: .preferredColorScheme(themeManager.colorScheme)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared MS tokens to DesignTokens.swift</name>
  <read_first>
    - Circles/DesignSystem/DesignTokens.swift
    - Circles/Home/HomeView.swift
  </read_first>
  <files>
    Circles/DesignSystem/DesignTokens.swift
  </files>
  <action>
    Append a new MARK section to DesignTokens.swift after the existing content.
    Do NOT modify existing tokens (darkBackground, accent, AppColors, etc.) — only add new content.

    Add this block at the bottom of DesignTokens.swift:

    ```swift
    // MARK: - Midnight Sanctuary Color Tokens

    /// Shared module-level tokens. All files in the Circles target can use these
    /// without any import — same module. Use explicit `Color.msToken` prefix (not dot-syntax)
    /// because Xcode 26 / Swift 6 cannot infer Color from ShapeStyle context.
    extension Color {

        // Backgrounds
        /// Primary app background — deep forest green
        static let msBackground     = Color(hex: "1A2E1E")
        /// Deeper layered background — used as inner base
        static let msBackgroundDeep = Color(hex: "131C14")

        // Card surfaces
        /// Shared/accountable habit card
        static let msCardShared     = Color(hex: "243828")
        /// Personal/private habit card — same as msCardDeep
        static let msCardDeep       = Color(hex: "1E3122")
        /// Shared habit — completed warmth
        static let msCardDone       = Color(hex: "2A4A30")
        /// Personal habit card — sanctuary warmth
        static let msCardWarm       = Color(hex: "201C14")
        /// Personal habit — completed warmth
        static let msCardWarmDone   = Color(hex: "2E2410")

        // Accent
        /// Gold accent — CTAs, icons, highlights
        static let msGold           = Color(hex: "D4A240")

        // Text
        /// Cream primary text
        static let msTextPrimary    = Color(hex: "F0EAD6")
        /// Sage muted text — labels, secondary copy
        static let msTextMuted      = Color(hex: "8FAF94")

        // Border
        /// Subtle gold border at 18% opacity
        static let msBorder         = Color(hex: "D4A240").opacity(0.18)
    }
    ```

    After writing: verify the file compiles by running:
      xcodebuild -project Circles/Circles.xcodeproj -scheme Circles -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E "error:|BUILD"

    At this stage, expect duplicate definition compiler errors because the private extensions
    still exist in the individual files. That is EXPECTED — proceed to Task 2.
    If the error is something other than "invalid redeclaration", stop and investigate.
  </action>
  <verify>
    <automated>grep -n "static let msBackground\|static let msGold\|static let msCardDeep\|static let msCardWarmDone" Circles/DesignSystem/DesignTokens.swift</automated>
  </verify>
  <acceptance_criteria>
    - DesignTokens.swift contains all 11 ms* token definitions
    - All tokens are in an `extension Color` block (not private)
    - File is syntactically valid Swift (no parse errors)
  </acceptance_criteria>
  <done>DesignTokens.swift has the complete shared MS token extension</done>
</task>

<task type="auto">
  <name>Task 2: Remove private extension Color blocks from all 29 files, fix msCardPersonal references</name>
  <read_first>
    - Circles/Home/HomeView.swift
    - Circles/Home/HabitDetailView.swift
    - Circles/Home/AddPrivateIntentionSheet.swift
    - Circles/Auth/AuthView.swift
    - Circles/Community/CommunityView.swift
    - Circles/Circles/CircleDetailView.swift
    - Circles/Circles/CreateCircleView.swift
    - Circles/Circles/JoinCircleView.swift
    - Circles/Moment/MomentCameraView.swift
    - Circles/Moment/MomentPreviewView.swift
    - Circles/Profile/ProfileView.swift
    - Circles/Onboarding/AmiirLandingSanctuaryView.swift
    - Circles/Onboarding/AmiirStep3PersonalView.swift
    - Circles/Onboarding/AmiirStep1IdentityView.swift
    - Circles/Onboarding/AmiirStep2HabitsView.swift
    - Circles/Onboarding/AmiirStep3LocationView.swift
    - Circles/Onboarding/AmiirAIGenerationView.swift
    - Circles/Onboarding/AmiirActivationView.swift
    - Circles/Onboarding/OnboardingTransitionView.swift
    - Circles/Onboarding/CirclePreviewView.swift
    - Circles/Onboarding/JoinerLandingView.swift
    - Circles/Onboarding/JoinerCircleAlignmentView.swift
    - Circles/Onboarding/JoinerPersonalHabitsView.swift
    - Circles/Onboarding/JoinerAIGenerationView.swift
    - Circles/Onboarding/JoinerIdentityView.swift
    - Circles/Onboarding/JoinerAuthGateView.swift
  </read_first>
  <files>
    Circles/Home/HomeView.swift,
    Circles/Home/HabitDetailView.swift,
    Circles/Home/AddPrivateIntentionSheet.swift,
    Circles/Auth/AuthView.swift,
    Circles/Community/CommunityView.swift,
    Circles/Circles/CircleDetailView.swift,
    Circles/Circles/CreateCircleView.swift,
    Circles/Circles/JoinCircleView.swift,
    Circles/Moment/MomentCameraView.swift,
    Circles/Moment/MomentPreviewView.swift,
    Circles/Profile/ProfileView.swift,
    Circles/Onboarding/AmiirLandingSanctuaryView.swift,
    Circles/Onboarding/AmiirStep3PersonalView.swift,
    Circles/Onboarding/AmiirStep1IdentityView.swift,
    Circles/Onboarding/AmiirStep2HabitsView.swift,
    Circles/Onboarding/AmiirStep3LocationView.swift,
    Circles/Onboarding/AmiirAIGenerationView.swift,
    Circles/Onboarding/AmiirActivationView.swift,
    Circles/Onboarding/OnboardingTransitionView.swift,
    Circles/Onboarding/CirclePreviewView.swift,
    Circles/Onboarding/JoinerLandingView.swift,
    Circles/Onboarding/JoinerCircleAlignmentView.swift,
    Circles/Onboarding/JoinerPersonalHabitsView.swift,
    Circles/Onboarding/JoinerAIGenerationView.swift,
    Circles/Onboarding/JoinerIdentityView.swift,
    Circles/Onboarding/JoinerAuthGateView.swift
  </files>
  <action>
    For EACH of the 26 files listed above:

    Step A — Read the file.

    Step B — Find the `private extension Color { ... }` block. It starts with:
        `private extension Color {`
      and ends with the closing `}` that matches it. Remove the entire block
      (including blank lines immediately before/after that were used for spacing).

    Step C — In HomeView.swift ONLY: rename all usages of `Color.msCardPersonal` to
      `Color.msCardDeep` — they have the same hex value, just consolidating to one name.
      grep -n "msCardPersonal" Circles/Home/HomeView.swift — find all lines and update.

    Step D — In HomeView.swift ONLY: if there are any remaining usages of `Color.msCardDeep`
      that were previously `msCardDeep` in the private extension (HabitDetailView,
      AddPrivateIntentionSheet) — these already use the correct name, no rename needed.

    Step E — Write the modified file back. The `Color.ms*` usages in the body remain
      unchanged — they will now resolve to the shared DesignTokens extension instead
      of the removed private extension.

    Process files in this order to detect compilation errors early:
      1. AuthView.swift (simplest, fewest tokens)
      2. OnboardingTransitionView.swift
      3. JoinerLandingView.swift, JoinerCircleAlignmentView.swift, JoinerPersonalHabitsView.swift,
         JoinerAIGenerationView.swift, JoinerIdentityView.swift, JoinerAuthGateView.swift
      4. AmiirLandingSanctuaryView.swift, AmiirStep1IdentityView.swift, AmiirStep2HabitsView.swift,
         AmiirStep3LocationView.swift, AmiirStep3PersonalView.swift, AmiirAIGenerationView.swift,
         AmiirActivationView.swift, CirclePreviewView.swift
      5. CommunityView.swift, CreateCircleView.swift, JoinCircleView.swift
      6. MomentCameraView.swift, MomentPreviewView.swift
      7. ProfileView.swift, CircleDetailView.swift
      8. AddPrivateIntentionSheet.swift, HabitDetailView.swift
      9. HomeView.swift (most complex — msCardPersonal rename happens here)

    After processing every 5 files, run an incremental build check:
      xcodebuild -project Circles/Circles.xcodeproj -scheme Circles -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E "error:|BUILD"

    Fix any "invalid redeclaration" errors by confirming the private block was fully removed.
    Fix any "unresolved identifier" errors by confirming the token name matches DesignTokens.swift.

    Final build:
      xcodebuild -project Circles/Circles.xcodeproj -scheme Circles -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20
  </action>
  <verify>
    <automated>grep -rn "private extension Color" Circles/ --include="*.swift" | grep -v "^Binary"</automated>
  </verify>
  <acceptance_criteria>
    - grep returns no results — zero private Color extension blocks remain in any file
    - grep -rn "msCardPersonal" Circles/ --include="*.swift" returns no results
    - xcodebuild BUILD SUCCEEDED with zero errors
  </acceptance_criteria>
  <done>All 26 private Color extension blocks removed, msCardPersonal renamed to msCardDeep in HomeView, build clean</done>
</task>

<task type="auto">
  <name>Task 3: Simplify ThemeManager to dark-mode only, remove NOAA solar math</name>
  <read_first>
    - Circles/DesignSystem/ThemeManager.swift
    - Circles/CirclesApp.swift
    - Circles/ContentView.swift
  </read_first>
  <files>
    Circles/DesignSystem/ThemeManager.swift,
    Circles/CirclesApp.swift
  </files>
  <action>
    Step 1 — Rewrite ThemeManager.swift. Replace the entire file content with:

    ```swift
    import SwiftUI

    // MARK: - ThemeManager

    /// Enforces dark mode throughout the app.
    /// The Midnight Sanctuary palette is designed exclusively for dark mode.
    @Observable
    @MainActor
    final class ThemeManager {

        static let shared = ThemeManager()

        /// Always `.dark` — Midnight Sanctuary is dark-mode only.
        let colorScheme: ColorScheme = .dark

        private init() {}
    }
    ```

    Step 2 — Update CirclesApp.swift: remove the `themeManager.scheduleAutoSwitch()` call
    inside `.onAppear`. The `.onAppear` block likely also sets `AuthManager.sharedForAPNs`.
    Keep the APNs line, just remove the scheduleAutoSwitch line.

    Before editing CirclesApp.swift, read it carefully to preserve the APNs setup line.
    The updated .onAppear should be:
      .onAppear {
          AuthManager.sharedForAPNs = authManager
      }

    Step 3 — ContentView.swift does NOT need changes — it reads `themeManager.colorScheme`
    which still works (now returns a constant `.dark`). Do NOT touch ContentView.swift.

    Step 4 — Final build:
      xcodebuild -project Circles/Circles.xcodeproj -scheme Circles -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20
  </action>
  <verify>
    <automated>grep -n "SolarCalculator\|ThemeMode\|scheduleAutoSwitch\|sunriseTimer\|sunsetTimer\|NOAA" Circles/DesignSystem/ThemeManager.swift Circles/CirclesApp.swift</automated>
  </verify>
  <acceptance_criteria>
    - grep returns no results — no NOAA/solar/timer references remain
    - ThemeManager.swift is < 20 lines
    - grep -n "colorScheme = .dark" Circles/DesignSystem/ThemeManager.swift returns 1 line
    - xcodebuild BUILD SUCCEEDED with zero errors
  </acceptance_criteria>
  <done>ThemeManager simplified to 15-line dark-mode enforcer, NOAA solar math deleted, build clean</done>
</task>

</tasks>

<verification>
Complete verification after all 3 tasks:

  # 1. No private Color blocks remain
  grep -rn "private extension Color" Circles/ --include="*.swift"

  # 2. No msCardPersonal remains
  grep -rn "msCardPersonal" Circles/ --include="*.swift"

  # 3. All MS tokens are in DesignTokens.swift
  grep -n "static let ms" Circles/DesignSystem/DesignTokens.swift

  # 4. ThemeManager is minimal
  wc -l Circles/DesignSystem/ThemeManager.swift

  # 5. Final clean build
  xcodebuild -project Circles/Circles.xcodeproj -scheme Circles -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5

All grep commands must return expected results. Build must succeed.
</verification>

<success_criteria>
- DesignTokens.swift has the single authoritative MS token extension (11 tokens)
- Zero files have `private extension Color` blocks
- ThemeManager.swift is dark-mode only (no ThemeMode enum, no SolarCalculator, no timers)
- `scheduleAutoSwitch()` removed from CirclesApp.swift
- `msCardPersonal` renamed to `msCardDeep` throughout
- xcodebuild BUILD SUCCEEDED with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-codebase-cleanup/12-03-SUMMARY.md`
</output>
