---
phase: 12-codebase-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Circles/Models/HalaqaMember.swift
  - Circles/Moment/MomentCardView.swift
  - Circles/Circles/JoinFromLinkView.swift
  - Circles/DesignSystem/AppIconView.swift
  - Circles/Onboarding/OnboardingCoordinator.swift
  - Circles/Onboarding/ProfileSetupView.swift
  - Circles/Onboarding/RamadanAmountView.swift
  - Circles/Onboarding/AIStepDownView.swift
  - Circles/Onboarding/HabitSelectionView.swift
  - Circles/Onboarding/AmiirStep4SoulGateView.swift
  - Circles/Onboarding/MemberStep1HabitsView.swift
  - Circles/Onboarding/MemberStep2LocationView.swift
autonomous: true
requirements:
  - CLEANUP-01

must_haves:
  truths:
    - "Build compiles with zero errors after all deletions"
    - "No Swift file in the project references HalaqaMember, MomentCardView, JoinFromLinkView, AppIconView, OnboardingCoordinator (old), ProfileSetupView, RamadanAmountView, AIStepDownView, HabitSelectionView, AmiirStep4SoulGateView, MemberStep1HabitsView, or MemberStep2LocationView"
    - "Xcode project file (.pbxproj) no longer references the deleted Swift files"
  artifacts:
    - path: "Circles/Circles.xcodeproj/project.pbxproj"
      provides: "Updated project file with deleted files removed"
      contains: "no HalaqaMember.swift"
  key_links:
    - from: "Circles.xcodeproj/project.pbxproj"
      to: "deleted Swift files"
      via: "file reference removal"
      pattern: "HalaqaMember|MomentCardView|JoinFromLinkView|AppIconView"
---

<objective>
Delete 12 confirmed-dead Swift files: 4 isolated dead files, the old OnboardingCoordinator cluster (5 files that only reference each other and the dead coordinator), and 3 old onboarding step views no longer referenced by any live code.

Purpose: Remove dead weight before token consolidation. Each deleted file is either self-contained with zero external references, or only referenced from other files being deleted in this same wave.

Output: 12 files removed from disk and from Xcode project file. Build compiles clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 4 isolated dead files (zero external references)</name>
  <read_first>
    - Circles/Models/HalaqaMember.swift
    - Circles/Moment/MomentCardView.swift
    - Circles/Circles/JoinFromLinkView.swift
    - Circles/DesignSystem/AppIconView.swift
  </read_first>
  <files>
    Circles/Models/HalaqaMember.swift,
    Circles/Moment/MomentCardView.swift,
    Circles/Circles/JoinFromLinkView.swift,
    Circles/DesignSystem/AppIconView.swift,
    Circles/Circles.xcodeproj/project.pbxproj
  </files>
  <action>
    Step 1 — Confirm zero external references before deleting (grep for each name):
      grep -rn "HalaqaMember" Circles/ --include="*.swift" | grep -v "HalaqaMember.swift"
      grep -rn "MomentCardView" Circles/ --include="*.swift" | grep -v "MomentCardView.swift"
      grep -rn "JoinFromLinkView" Circles/ --include="*.swift" | grep -v "JoinFromLinkView.swift"
      grep -rn "AppIconView" Circles/ --include="*.swift" | grep -v "AppIconView.swift"

    If any grep returns results, STOP and report — do not proceed with that file.

    Step 2 — Delete the 4 files from disk:
      rm Circles/Models/HalaqaMember.swift
      rm Circles/Moment/MomentCardView.swift
      rm Circles/Circles/JoinFromLinkView.swift
      rm Circles/DesignSystem/AppIconView.swift

    Step 3 — Remove the 4 file references from Circles.xcodeproj/project.pbxproj.
    The .pbxproj file contains PBXFileReference and PBXBuildFile entries for each Swift file.
    For each deleted file, find and remove:
      (a) The PBXFileReference block — a line like:
          XXXXXXXXXXXX /* HalaqaMember.swift */ = {isa = PBXFileReference; ...};
      (b) The PBXBuildFile block — a line like:
          XXXXXXXXXXXX /* HalaqaMember.swift in Sources */ = {isa = PBXBuildFile; ...};
      (c) The file reference in the PBXGroup children array — a bare UUID line inside the group that contains the file
      (d) The build phase reference in PBXSourcesBuildPhase files array — a bare UUID line

    Use sed or direct editing of project.pbxproj. Pattern for each file:
      grep -n "HalaqaMember\|MomentCardView\|JoinFromLinkView\|AppIconView" Circles/Circles.xcodeproj/project.pbxproj

    Remove every matching line. The .pbxproj format uses one UUID per line for references, so line-by-line deletion is safe.

    Step 4 — Build to verify:
      xcodebuild -project Circles/Circles.xcodeproj -scheme Circles -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20

    Expected: BUILD SUCCEEDED
  </action>
  <verify>
    <automated>grep -rn "HalaqaMember\|MomentCardView\|JoinFromLinkView\|AppIconView" Circles/ --include="*.swift" --include="*.pbxproj" | grep -v "^Binary"</automated>
  </verify>
  <acceptance_criteria>
    - grep returns no results (all 4 names gone from all files)
    - xcodebuild reports BUILD SUCCEEDED
    - Files no longer exist on disk: `ls Circles/Models/HalaqaMember.swift` returns "No such file"
  </acceptance_criteria>
  <done>4 files deleted from disk and project file, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Delete old OnboardingCoordinator cluster (5 files + 3 dead step views)</name>
  <read_first>
    - Circles/Onboarding/OnboardingCoordinator.swift
    - Circles/Onboarding/ProfileSetupView.swift
    - Circles/Onboarding/RamadanAmountView.swift
    - Circles/Onboarding/AIStepDownView.swift
    - Circles/Onboarding/HabitSelectionView.swift
    - Circles/Onboarding/AmiirStep4SoulGateView.swift
    - Circles/Onboarding/MemberStep1HabitsView.swift
    - Circles/Onboarding/MemberStep2LocationView.swift
  </read_first>
  <files>
    Circles/Onboarding/OnboardingCoordinator.swift,
    Circles/Onboarding/ProfileSetupView.swift,
    Circles/Onboarding/RamadanAmountView.swift,
    Circles/Onboarding/AIStepDownView.swift,
    Circles/Onboarding/HabitSelectionView.swift,
    Circles/Onboarding/AmiirStep4SoulGateView.swift,
    Circles/Onboarding/MemberStep1HabitsView.swift,
    Circles/Onboarding/MemberStep2LocationView.swift,
    Circles/Circles.xcodeproj/project.pbxproj
  </files>
  <action>
    Step 1 — Confirm no LIVE (non-deleted) file references these:
      grep -rn "\bOnboardingCoordinator\b" Circles/ --include="*.swift" | grep -v "OnboardingCoordinator.swift" | grep -v "ProfileSetupView\|AIStepDownView\|HabitSelectionView\|RamadanAmountView\|LocationPickerView"
      grep -rn "ProfileSetupView\|RamadanAmountView\|AIStepDownView\|HabitSelectionView\b" Circles/ --include="*.swift" | grep -v -E "(ProfileSetupView|RamadanAmountView|AIStepDownView|HabitSelectionView|OnboardingCoordinator)\.swift"
      grep -rn "AmiirStep4SoulGateView\|MemberStep1HabitsView\|MemberStep2LocationView" Circles/ --include="*.swift" | grep -v -E "(AmiirStep4SoulGateView|MemberStep1HabitsView|MemberStep2LocationView)\.swift"

    If any grep returns results from a file NOT in the deletion list, STOP and report.

    Step 2 — NOTE: LocationPickerView.swift is NOT deleted here because its static `cities` array is
    still used by AmiirStep3LocationView.swift and JoinerIdentityView.swift. Only the struct View itself
    goes unused — but deleting it would break compilation. Leave LocationPickerView.swift in place.

    Step 3 — Delete the 8 files:
      rm Circles/Onboarding/OnboardingCoordinator.swift
      rm Circles/Onboarding/ProfileSetupView.swift
      rm Circles/Onboarding/RamadanAmountView.swift
      rm Circles/Onboarding/AIStepDownView.swift
      rm Circles/Onboarding/HabitSelectionView.swift
      rm Circles/Onboarding/AmiirStep4SoulGateView.swift
      rm Circles/Onboarding/MemberStep1HabitsView.swift
      rm Circles/Onboarding/MemberStep2LocationView.swift

    Step 4 — Remove all 8 file references from Circles.xcodeproj/project.pbxproj.
    For each deleted file, remove:
      (a) PBXFileReference line containing the filename
      (b) PBXBuildFile line containing the filename
      (c) UUID-only lines in PBXGroup children blocks that reference those UUIDs
      (d) UUID-only lines in PBXSourcesBuildPhase files blocks that reference those UUIDs

    grep -n "OnboardingCoordinator\|ProfileSetupView\|RamadanAmountView\|AIStepDownView\|HabitSelectionView\|AmiirStep4SoulGate\|MemberStep1Habits\|MemberStep2Location" Circles/Circles.xcodeproj/project.pbxproj

    Then use the UUIDs found in (a)/(b) to find and remove the bare UUID lines in (c)/(d).

    Step 5 — Build to verify:
      xcodebuild -project Circles/Circles.xcodeproj -scheme Circles -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20

    Expected: BUILD SUCCEEDED
  </action>
  <verify>
    <automated>grep -rn "OnboardingCoordinator\|ProfileSetupView\|RamadanAmountView\|AIStepDownView\|HabitSelectionView\b\|AmiirStep4SoulGateView\|MemberStep1HabitsView\|MemberStep2LocationView" Circles/ --include="*.swift" --include="*.pbxproj" | grep -v "^Binary"</automated>
  </verify>
  <acceptance_criteria>
    - grep returns no results (all 8 names purged)
    - xcodebuild reports BUILD SUCCEEDED
    - LocationPickerView.swift still exists on disk (NOT deleted)
    - AmiirStep3LocationView.swift compiles — it uses LocationPickerView.cities
  </acceptance_criteria>
  <done>8 dead files deleted, project file updated, build succeeds with zero errors</done>
</task>

<task type="human">
  <name>Task 3: Simulator smoke test — confirm no runtime regressions</name>
  <read_first>
    - (none — this is a manual verification step)
  </read_first>
  <files></files>
  <action>
    A clean build does not guarantee runtime correctness. This task confirms the app
    actually launches and the key screens render after the deletions.

    Step 1 — Boot the simulator and launch:
      open -a Simulator
      xcrun simctl boot "iPhone 16" 2>/dev/null || true
      xcodebuild -project Circles/Circles.xcodeproj -scheme Circles \
        -destination 'platform=iOS Simulator,name=iPhone 16' \
        -derivedDataPath /tmp/circles-dd build 2>&1 | tail -5
      xcrun simctl install booted /tmp/circles-dd/Build/Products/Debug-iphonesimulator/Circles.app
      xcrun simctl launch booted app.joinlegacy

    Step 2 — Manually navigate in the Simulator and confirm each screen renders:
      [ ] Home tab (Daily Intentions) — cards visible, no blank white screen
      [ ] Tap a habit → HabitDetailView opens — icon shows correctly, not raw text
      [ ] Community tab — feed or gate loads, no crash
      [ ] Circles tab (if separate) — circle cards visible
      [ ] Profile tab — avatar area and stats visible

    Step 3 — Specifically verify the deleted files left no holes:
      [ ] Nothing in the app references old onboarding via a stale deep link or
          navigation — open the app fresh as a new user path if possible (or
          confirm ContentView routes correctly without touching old coordinator)

    If any screen shows a blank/white view or the app crashes: STOP, do not
    write the SUMMARY, and report the exact screen + error before committing.
  </action>
  <verify>
    <automated>xcrun simctl launch booted app.joinlegacy 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - App launches without crashing on the simulator
    - Home tab renders habit cards (not blank)
    - Community tab renders (feed or reciprocity gate — not blank)
    - Profile tab renders (not blank)
    - No "Thread 1: EXC_BAD_ACCESS" or crash dialog appears
  </acceptance_criteria>
  <done>App launches, all 5 tabs render without blank screens or crashes</done>
</task>

</tasks>

<verification>
After all tasks complete:
  grep -rn "HalaqaMember\|MomentCardView\|JoinFromLinkView\|AppIconView\|OnboardingCoordinator\|ProfileSetupView\|RamadanAmountView\|AIStepDownView\|HabitSelectionView\b\|AmiirStep4SoulGateView\|MemberStep1HabitsView\|MemberStep2LocationView" Circles/ --include="*.swift" | grep -v "^Binary"

Must return no output. Then confirm clean build one final time.
</verification>

<success_criteria>
- All 12 dead files removed from disk
- Xcode project file (.pbxproj) has zero dangling references to deleted files
- xcodebuild BUILD SUCCEEDED with zero errors
- LocationPickerView.swift intact (used by AmiirStep3LocationView and JoinerIdentityView)
</success_criteria>

<output>
After completion, create `.planning/phases/12-codebase-cleanup/12-01-SUMMARY.md`
</output>
