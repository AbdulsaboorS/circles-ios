# Plan 11.4-01 — SUMMARY

**Status:** Complete
**Commit:** f4e0ff5

## What was built

### Task 1: RLS Migration (applied via Supabase MCP)
- Dropped any stale INSERT/SELECT policies on `circle_moments`
- Added `users_insert_own_moments`: INSERT allowed when `auth.uid() = user_id` AND user is a circle member
- Added `circle_members_select_moments`: SELECT allowed via `auth_user_circle_ids()`
- Added `authenticated_upload_circle_moments`: INSERT to `storage.objects` for `circle-moments` bucket (authenticated users)
- Added `authenticated_select_circle_moments`: SELECT from `storage.objects` for `circle-moments` bucket

### Task 2: Edge Function Rewrite
- `send-moment-window-notifications/index.ts` fully rewritten
- Now reads `daily_moments` table (by today's UTC date) for `prayer_name` — removed dependency on `circles.prayer_time`
- Returns early if no `daily_moments` row exists for today
- Deduplicates to one notification per user across multiple circle memberships
- Notification body updated to warm Islamic copy: "Time to capture this moment. Your circle is waiting. ✨"

## Acceptance criteria
- ✓ `daily_moments` queried for today's prayer
- ✓ `circles.prayer_time` no longer referenced
- ✓ `userMap` deduplication present
- ✓ Warm Islamic copy in notification body
- ✓ Migration applied and verified in Supabase (INSERT + SELECT on circle_moments; INSERT + SELECT on storage.objects)
