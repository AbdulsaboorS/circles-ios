---
status: testing
phase: 11.4-circle-moment
source: [11.4-01-SUMMARY.md, 11.4-02-SUMMARY.md, 11.4-03-SUMMARY.md, 11.4-04-SUMMARY.md]
started: 2026-04-04T00:00:00Z
updated: 2026-04-04T00:00:00Z
---

## Current Test

number: 1
name: Profile gear icon visible + opens settings sheet
expected: |
  Open the Profile tab. In the top-right of the navigation bar, a gear icon (⚙️) appears.
  Tapping it slides up a sheet titled "Settings" containing the same rows as the inline
  settings section (Edit Profile, Notifications, Location & Prayer Times) plus the Sign Out button.
awaiting: user response

## Tests

### 1. Profile gear icon visible + opens settings sheet
expected: Open the Profile tab. In the top-right of the navigation bar, a gear icon (⚙️) appears. Tapping it slides up a sheet titled "Settings" containing the same rows as the inline settings section (Edit Profile, Notifications, Location & Prayer Times) plus the Sign Out button.
result: issue
reported: "Settings sheet opens but rows are not clickable. Settings rows also appear inline on profile page — should be removed from profile; profile should only show avatar, stats, circles. Settings only in gear sheet."
severity: major
fix: Removed settingsSection from main ScrollView body (profile now shows only avatarSection + statsCard). Settings live exclusively in the gear sheet. Build succeeded.

### 2. Moment feed card full-width photo
expected: In the community feed (or circle detail feed), any Circle Moment card shows its photo filling the full card width — no horizontal side margins on the photo. The photo is taller than wide (3:4 portrait ratio). The card corners are still rounded.
result: [pending]

### 3. Late badge only — no star for on-time
expected: For a moment posted LATE (outside the prayer window), the card shows a small "🕰 Late" pill badge in the top-right corner of the photo. For a moment posted ON TIME, NO badge appears at all (no star, no text). If you only have late moments, check that the star is gone.
result: [pending]

### 4. No star badge on circle detail moment strip
expected: Open any Circle Detail view. The horizontal moment strip near the top (the row of small thumbnails for each member) shows no gold star badge on any card — even for on-time moments. Cards show the photo cleanly with no overlaid star icon.
result: [pending]

### 5. Moment preview disclaimer shows circle count
expected: From CommunityView (Feed tab), tap the camera / open the global moment camera. Take or capture a photo to reach MomentPreviewView. If you are a member of 2+ circles, a disclaimer appears below the preview: "This will be shared to all your circles (N)" where N matches your circle count. If you are only in 1 circle, no disclaimer is shown (or N=1).
result: [pending]

### 6. Notification warm Islamic copy (edge function)
expected: The moment-window push notification body reads: "Time to capture this moment. Your circle is waiting. ✨" — warm Islamic tone, not a generic alert. (Skip if you cannot trigger a real prayer-window notification.)
result: skipped
reason: send-moment-window-notifications edge function not yet deployed to Supabase. Test deferred to dedicated push notification QA pass.

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0

## Gaps

[none yet]
