---
phase: 11.4-circle-moment
plan: 04
type: summary
status: complete
---

# 11.4-04 Summary — Wire Callers + Pin Own Moment

## What Was Done

### CommunityView.swift
- Preview sheet: removed `if let circleId` guard wrapper
- `postMoment(circleId:)` → `postMomentToAllCircles(circleIds: viewModel.circles.map { $0.id })`
- Partial failure surfaced as thrown NSError: "Posted to X of Y circles. N failed — tap Post again to retry."
- `circleCount` already passed from Plan 02

### CircleDetailView.swift
- Added `@State private var allUserCircleIds: [UUID] = []`
- `.task` block: after members fetch, calls `CircleService.shared.fetchMyCircles(userId:)` and populates `allUserCircleIds`; falls back to `[circle.id]` on failure
- Preview sheet: `circleCount: 1` stub → `circleCount: max(1, allUserCircleIds.count)`
- `postMoment(circleId:circle.id)` → `postMomentToAllCircles(circleIds: postCircleIds)` using `allUserCircleIds` (with `[circle.id]` fallback)
- `windowStart` passed as ISO8601 string converted from `DailyMomentService.shared.windowStart: Date?`
- Partial failure same NSError pattern

### FeedViewModel.swift
- Added `pinOwnMoments(_:currentUserId:) -> [FeedItem]` static helper — moves `.moment` items where `userId == currentUserId` to the front, preserving relative order
- In `loadInitial`: `items = singleCircleId != nil ? Self.pinOwnMoments(firstPage, currentUserId:) : firstPage`
- Global feed unaffected (no pinning when `singleCircleId == nil`)

## Build Status
BUILD SUCCEEDED (iPhone 17 Pro simulator)
