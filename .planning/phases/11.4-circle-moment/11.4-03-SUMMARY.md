---
phase: 11.4-circle-moment
plan: 03
type: summary
status: complete
---

# 11.4-03 Summary — Feed Card Full-Width + Late Badge + Profile Gear

## What Was Done

### MomentFeedCard.swift
- Photo `frame(height: 420)` → `aspectRatio(3.0 / 4.0, contentMode: .fill)` with `frame(maxWidth: .infinity)` — photo now fills full card width
- Badge overlay replaced: on-time posts show **no badge**; late posts show `🕰 Late` (Capsule pill, sage text)
- Removed `star.fill` and "Posted at [prayer]" on-time badge entirely

### MomentCardView.swift
- Removed `if moment.isOnTime { ZStack { ... star.fill ... } }` block from `unlockedCard`
- Simplified `unlockedCard` — ZStack wrapper removed, AsyncImage stands alone with aspectRatio + clipShape

### ProfileView.swift
- Added `@State private var showSettingsSheet = false`
- Added `.toolbar { ToolbarItem(placement: .navigationBarTrailing) { gearshape.fill button } }`
- Added `.sheet(isPresented: $showSettingsSheet)` presenting `settingsSheetContent`
- Added `settingsSheetContent` computed var — reuses existing `settingsSection` inside an MS-themed ZStack with "Settings" title

## Build Status
BUILD SUCCEEDED (iPhone 17 Pro simulator)
