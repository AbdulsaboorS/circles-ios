{
  "permissions": {
    "defaultMode": "bypassPermissions"
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "STATE=$(head -40 .planning/STATE.md 2>/dev/null || echo ''); [ -z \"$STATE\" ] && exit 0; printf '## Circles iOS — Auto-loaded Project Context\\n\\n### Current State\\n%s' \"$STATE\" | jq -Rs '{hookSpecificOutput: {hookEventName: \"SessionStart\", additionalContext: .}}'",
            "statusMessage": "Loading project context..."
          }
        ]
      }
    ]
  }
}
